# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/shiv_accounts?schema=public"

# Example for local development
# DATABASE_URL="postgresql://postgres:password@localhost:5432/shiv_accounts?schema=public"

# Example for cloud database (Supabase, Neon, etc.)
# DATABASE_URL="postgresql://username:password@hostname:5432/database_name?schema=public"

# Firebase Configuration (if using Firestore)
FIREBASE_PROJECT_ID="your-firebase-project-id"
FIREBASE_CLIENT_EMAIL="your-service-account-email"
FIREBASE_PRIVATE_KEY="your-private-key"

# Application Configuration
NODE_ENV="development"
PORT=3001
JWT_SECRET="your-jwt-secret-key"

# File Upload Configuration
MAX_FILE_SIZE=5242880 # 5MB in bytes
UPLOAD_PATH="./uploads"
